# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/airbus_project
PGHOST=localhost
PGPORT=5432
PGDATABASE=airbus_project
PGUSER=your_username
PGPASSWORD=your_password

# Authentication
SESSION_SECRET=your_strong_session_secret_here
JWT_SECRET=your_strong_jwt_secret_here

# Server Configuration
PORT=5000
NODE_ENV=development

# CORS Configuration for Local Development
# Add your frontend URLs separated by commas
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:8080

# Frontend Environment Variables (create client/.env file with these)
# VITE_API_URL=http://localhost:5000
# For separate backend servers, use the actual backend port:
# VITE_API_URL=http://localhost:3001